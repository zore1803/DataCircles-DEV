version: "3.8"
services:
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--requirepass", "datacircles123"]
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
volumes:
  redis-data:
